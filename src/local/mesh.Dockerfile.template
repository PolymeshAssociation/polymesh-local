# Adds libfaketime to Polymesh image. Allows for old snapshots to be alive.

FROM polymathnet/polymesh:{{VERSION}}-debian
USER root
RUN apt-get update && apt-get install -y libfaketime node.js

COPY fast-forward.js /fast-forward.js

ENTRYPOINT ["/usr/bin/node", "/fast-forward.js"]
